/**
 * Utility for merging Tailwind CSS classes.
 * Re-exports the cn utility from @heroui/theme for consistent usage across the app.
 *
 * This centralized import makes it easier to replace the implementation in the future if needed.
 */
export { cn } from '@heroui/theme'
